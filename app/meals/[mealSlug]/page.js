const MealDetailsPage = ({ title, slug, image, summary, creator }) => {
  // const { title, slug, image, summary, creator } = params
  // console.log(params);
 
  return <div>{title}</div>;
};

export default MealDetailsPage;
